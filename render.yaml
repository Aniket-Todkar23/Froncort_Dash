services:
  - type: web
    name: quill-sync-socket-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server/socket-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
